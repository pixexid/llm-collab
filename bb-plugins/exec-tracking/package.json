{
  "name": "bb-plugin-exec-tracking",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Execution tracking for llm-collab. First capability: records the thread's creation-time DEFAULT execution options (provider, model, reasoning_level) — labelled creation_defaults, NOT executed evidence (GH-695 P1-B; client/turn/requested sourcing is a tracked re-scope).",
  "engines": {
    "bb": ">=0.9",
    "bbPluginSdk": "^0.4.1"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "bb": {
    "name": "Exec Tracking",
    "description": "Records the thread's creation-time DEFAULT execution options (provider, model, reasoning_level). First capability of the task/execution-tracking plugin (GH-630/GH-617). The row is labelled creation_defaults — defaults, not executed evidence (GH-695 P1-B).",
    "branding": { "icon": "ClipboardList" },
    "server": "./server.ts"
  }
}
