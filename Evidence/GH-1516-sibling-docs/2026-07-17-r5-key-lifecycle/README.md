# GH-1516 sibling-docs evidence — R5 key-lifecycle link
Chains from the R4 link: this BEFORE == R4 AFTER. AFTER == live after the R5 fold
(marketing generation-key lifecycle reconciled to settlement-evidence: same-key 409 + pre-claim 429 retain).
openclaw_bots/amiga-operator-cmo.md unchanged this round (kept at abstraction per Codex).
