# GH-1516 sibling-docs evidence — R4 narrow-recovery link

Chains from the parent bundle: this link's BEFORE == parent AFTER.sha256 (verified identical).
AFTER == live files after the R4 fix (recovery scoped to provider_outcome_unknown; start vs settle
separated; no false auto-recovery).

- BEFORE.sha256 / AFTER.sha256 — sha256 of both docs each side
- SCOPED.diff — before -> after unified diff
- Files: marketing/service_notes_system_spec.md, openclaw_bots/amiga-operator-cmo.md
